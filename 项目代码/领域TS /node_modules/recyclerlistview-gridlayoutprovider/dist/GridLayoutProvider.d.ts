import { Dimension, Layout, LayoutProvider, LayoutManager } from "recyclerlistview";
export declare class GridLayoutProvider extends LayoutProvider {
    private _getHeightOrWidth;
    private _getSpan;
    private _maxSpan;
    private _renderWindowSize?;
    private _isHorizontal?;
    constructor(maxSpan: number, getLayoutType: (index: number) => string | number, getSpan: (index: number) => number, getHeightOrWidth: (index: number) => number);
    newLayoutManager(renderWindowSize: Dimension, isHorizontal?: boolean, cachedLayouts?: Layout[]): LayoutManager;
    private setLayout(dimension, index);
}
